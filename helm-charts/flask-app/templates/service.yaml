apiVersion: v1
kind: Service
metadata:
  name: flask-app
  labels:
    app: flask-app
spec:
  type: NodePort
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30420
      name: http
  selector:
    app: flask-app